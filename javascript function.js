//CSS:
//# -> identifies element's by it's ID
//. -> identifies element by it's CLASS


//document.getElementById("count-el").innerText;
//document.getElementById("count-el").textContent;  :Using .textContent because .innerText can skip human unreadable elements e.g. <space>
//document.querySelector("#sum-el") OR document.querySelector(".sum-el"); :Pass in the CSS selector